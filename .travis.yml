language: go
sudo: required

os:
  - linux
  - osx

go:
  - 1.12.x
  - master

env:
  matrix:
    - GO111MODULE=on

matrix:
  fast_finish: true
  allow_failures:
    - go: master

before_script:
  - go get github.com/prometheus/prometheus/cmd/promtool

script:
  - go test ./...
